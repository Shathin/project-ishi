name: auth_repo
description: Dart package that provides a password-only protection to the app using Shared Preferences and Crypto
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.5
  crypto: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  