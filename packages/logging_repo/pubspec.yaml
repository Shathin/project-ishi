name: logging_repo
description: Dart package that provides a logging capabilities
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.0.1
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  